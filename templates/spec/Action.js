'use strict';

import <%= classedName %> from 'actions/<%= classedFileName %>';

describe('<%= classedName %>', () => {
  it('should be defined', () => {
    expect(classedName).toBeDefined();
  });
});
